#!/bin/bash
set -e

parent=${PWD##*/}
repo=$1

if [ $# -eq 1 ]
  then
    filetype='md'
  else
    filetype=$2
    arg=$3
fi

mkcloudrepo $repo
pushd $repo
  mdinit $filetype $arg
  git add .
  git commit -m "init $filetype repo"
  git push --set-upstream origin master
popd
