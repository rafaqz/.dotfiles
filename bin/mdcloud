#!/bin/bash
set -e

parent=$1
repo=$2
filetype=$3
arg=$4
cloudrepo $parent $repo

pushd $repo
  mdinit $filetype $arg
  git add .
  git commit -m "init $filetype repo"
  git push --set-upstream origin master
popd
