#!/bin/bash
file=$1
ext="${file##*.}"

echo -n "Words: "
if [ $ext = "pdf" ]; then
  # pdftotext: - is for output to stdout instead of file
else
  paper $file | wc -w
  pandoc -t plain $file | sed -n '/^introduction/I,/^appendix\|^references\|^bibliography/p' | wc -w
fi
